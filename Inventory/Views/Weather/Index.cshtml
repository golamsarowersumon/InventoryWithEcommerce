@model IEnumerable<Domain.ViewModels.WeatherViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WeatherName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WeatherName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WeatherId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WeatherId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WeatherId })
        </td>
    </tr>
}

</table>
