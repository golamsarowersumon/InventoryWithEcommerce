@model IEnumerable<Domain.ViewModels.CelebrationViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CelebrationName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CelebrationType)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CelebrationName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CelebrationType)
        </td>
     
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CelebrationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CelebrationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CelebrationId })
        </td>
    </tr>
}

</table>
