@model IEnumerable<Domain.ViewModels.EducationQualificationViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EducationQualificationName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EducationQualificationName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EducationQualificationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EducationQualificationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EducationQualificationId })
        </td>
    </tr>
}

</table>
