@model IEnumerable<Domain.ViewModels.NationalityViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NationalityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NationalityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NationalityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.NationalityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NationalityID })
        </td>
    </tr>
}

</table>
