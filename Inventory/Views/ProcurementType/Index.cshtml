@model IEnumerable<Domain.ViewModels.ProcurementTypeViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProcurementTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProcurementTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProcurementTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProcurementTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProcurementTypeId })
        </td>
    </tr>
}

</table>
