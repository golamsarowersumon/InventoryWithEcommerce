@model IEnumerable<Domain.ViewModels.MaritalStatusViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaritalStatusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaritalStatusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaritalStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaritalStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaritalStatusId })
        </td>
    </tr>
}

</table>
